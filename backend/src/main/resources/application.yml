server:
  port: 3000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5462/customer
    username: arnold
    password: password
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
  main:
    web-application-type: servlet
